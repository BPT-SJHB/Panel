import { CheckedToken } from '../model/ticket-auth.model';

export const mockTicketTokenChecked: CheckedToken = {
  valid: true,
  tokenType: 'guest',
  phoneNumber: '09382579172',
  phoneVerified: true,
};
