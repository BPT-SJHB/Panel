const apiBaseUrl = "https://example.com";
const apiPort = 443;

export const environment = {
  production: false,
  apiUrl: `${apiBaseUrl}:${apiPort}`,
  mockTest: false, // این قسمت برای من هست که دسترسی به api ندارم
};
